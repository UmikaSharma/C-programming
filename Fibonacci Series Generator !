#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int num,a=0,b=1,c,i;
    scanf("%d",&num);
    if(num<=1){
        printf("%d ",a);
    }
    else{
        printf("%d %d ",a,b);
    for(i=3;i<=num;i++){
        c=a+b;
        printf("%d ",c);
        a=b;
        b=c;
    }
    }

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
