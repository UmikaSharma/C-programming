#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n;
    scanf("%d",&n);
    int arr[n];
    int v[n];
    for(int i=0;i<n;i++){
        scanf("%d",&arr[i]);
        v[i]=0;
    }
    int found=0;
    for(int i=0;i<n;i++){
        if(v[i]==1)
            continue;
        int count=1;
        for(int j=i+1;j<n;j++){
            if(arr[i]==arr[j]){
                v[j]=1;
                count++;
            }
        }
    
        if(count==1){
            printf("%d ",arr[i]);
            found=1;
        }
    }
        if(found==0){
            printf("No unique elements");
        }
        
    
    

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
