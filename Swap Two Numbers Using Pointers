#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n,k;
    int *p1=&n;
    int *p2=&k;
    scanf("%d %d",&n,&k);
    int temp=*p1;
    *p1=*p2;
    *p2=temp;
    printf("%d %d",n,k);

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
