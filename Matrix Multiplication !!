#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int r,c,r1,c1;
    scanf("%d %d",&r,&c);
    int arr[r][c];
    for(int i=0;i<r;i++){
        for(int j=0;j<c;j++){
            scanf("%d",&arr[i][j]);
        }
    }
    scanf("%d %d",&r1,&c1);
    int a[r1][c1];
    for(int i=0;i<r1;i++){
        for(int j=0;j<c1;j++){
            scanf("%d",&a[i][j]);
        }
    }
    if(c!=r1){
        printf("Matrix multiplication not possible");
        return 0;
    }
    int res[r][c1];
    
    for(int i=0;i<r;i++){
            for(int j=0;j<c1;j++){
                res[i][j]=0;
                for(int k=0;k<c;k++){
                    res[i][j]+=arr[i][k]*a[k][j];
                }
            }
        }
    
    for(int i=0;i<r;i++){
            for(int j=0;j<c1;j++){
                printf("%d ",res[i][j]);
            }
            printf("\n");
    }

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
