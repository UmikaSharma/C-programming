#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int r,c;
    scanf("%d %d",&r,&c);
    int arr[r][c];
    int a[r][c];
    int a2[r][c];
    for(int i=0;i<r;i++){
        for(int j=0;j<c;j++){
            scanf("%d",&arr[i][j]);
        }
    }
    for(int i=0;i<r;i++){
        for(int j=0;j<c;j++){
            scanf("%d",&a[i][j]);
        }
    }
    for(int i=0;i<r;i++){
        for(int j=0;j<c;j++){
            a2[i][j] = arr[i][j]-a[i][j];
        }
    }
    for(int i=0;i<r;i++){
        for(int j=0;j<c;j++){
            printf("%d ",a2[i][j]);
        }
        printf("\n");
    }
        
    /* Enter our code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
