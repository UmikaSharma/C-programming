#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n,k;
    scanf("%d",&n);
    int arr[n];
    for(int i =0;i<n;i++){
         scanf("%d",&arr[i]);
        }
    scanf("%d",&k);
    if(k<1||k>n){
        printf("Invalid position");
        return 0;
    }
    
    for (int i=0;i<n;i++){
        if(i!=k-1){
            printf("%d ",arr[i]);
        }
    }
    

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
